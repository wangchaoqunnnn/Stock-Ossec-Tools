# Stock-Ossec-Tools（老王量化 - 自建版）

> 参考 [https://stock.ossec.cn](https://stock.ossec.cn)（老王量化）主站实现的股票数据查询工具网页。
> 本项目的定位是：**免登录、免VIP、免投票、纯只读展示**的股票行情查询与分析工具。

---

## 一、需求文档

### 1. 项目背景

参考网站「老王量化」主站（`stock.ossec.cn`），该网站提供个股查询、投资组合、量化工具、行情中枢、复盘社区等多个模块。
本项目参考其页面结构与信息呈现方式，自建一套无需账号体系、无需付费订阅、无社区互动的精简版股票数据查询与分析工具。

### 2. 项目目标

- 提供一个可直接访问的网页，展示 A 股/亚太/美股/期货主要指数行情与个股行情。
- 支持按股票代码或名称快速检索个股，并展示完整行情详情与多维度分析。
- 提供投资组合、量化工具、行情中枢、复盘社区等辅助分析模块。
- 全部数据来源于公开行情接口（东方财富 / 腾讯），无任何账号、权限、付费门槛。

### 3. 功能需求

#### 3.1 首页（Home）
- 欢迎区：展示网站名称、简介与快速入口。
- 指数速览：展示主要指数实时行情卡片。
- 功能入口：提供个股查询、投资组合、量化工具、行情中枢、复盘社区五个模块的快捷入口。

#### 3.2 个股查询（Tools）
- 提供搜索输入框，支持按 **股票代码**（如 `600000`）或 **名称关键词**（如 `浦发`）模糊检索。
- 检索结果以下拉列表展示（代码 + 名称 + 市场类型），点击或回车后展示该股票的行情详情。
- 行情详情字段：现价、涨跌额、涨跌幅、今开、最高、最低、昨收、成交量（手）、成交额（元）、换手率、量比、市盈率(TTM)、市净率、总市值、流通市值、振幅、行业。
- 支持科创板、创业板股票搜索。
- **详情弹窗**：点击"详情"按钮打开，包含四个标签页：
  - **暗盘信息**：股票名称价格、市场排名、市场热度、买卖信号、强弱线、所属板块、所属概念标签、公司简述、行情走势图（分时/日K/周K/月K可切换）、暗盘资金统计（今日/本周/本月净流入）。
  - **个股分析**：综合评分圆环（技术面/基本面/资金面/情绪面四维度）、技术指标（MACD/KDJ/RSI/BOLL + MA5/MA10/MA20/MA60均线）、基本面数据（市盈率/市净率/ROE/营收/利润等）、资金流向（主力/散户/北向）、机构评级（评级/目标价/机构统计/最新研报）。
  - **暗盘历史**：汇总统计（近5日/10日/20日累计净流入、连续净流入天数）、历史数据表格（日期/收盘价/涨跌幅/暗盘流入/流出/净流入/趋势条形图）。
  - **异动清单**：汇总统计（今日异动次数/大单买入/快速拉升/放量突破）、异动记录列表（异动类型/时间/价格/涨跌幅/成交量/成交额/异动原因）。

#### 3.3 主要指数
- 指数卡片式展示，支持 **A股 / 亚太 / 美股 / 期货** 四个市场切换。
- 每个指数展示：名称、当前点位、涨跌额、涨跌幅。
- 支持60秒自动刷新。
- 指数范围：
  - A股：上证指数、深证成指、沪深300、创业板指、科创50、北证50
  - 亚太：恒生指数、恒生科技、国企指数、日经225、韩国KOSPI、澳洲ASX200、台湾加权
  - 美股：道琼斯、纳斯达克100、标普500
  - 期货：沪深300股指、上证50股指、中证500股指、中证1000股指、黄金期货、原油期货

#### 3.4 关注清单（Watchlist）
- 支持添加/删除关注股票，数据本地存储（localStorage）。
- 支持9列可配置展示：代码、名称、现价、涨跌幅、涨速、成交额、信号、备注、操作。
- 支持多维度排序（关注日期/现价/涨跌幅等）。
- 支持备注编辑。
- 支持批量添加股票。
- 支持60秒自动刷新行情。

#### 3.5 投资组合（Portfolio）
- **组合雷达**：展示投资组合的多维度雷达图分析。
- **收益排行榜**：展示组合内个股收益排名。
- **策略分类**：按不同投资策略分类展示组合。

#### 3.6 量化工具（Utility）
- **今日关注**：搜索框 + 日期选择 + 11列数据表格（代码/名称/现价/涨跌幅/涨速/成交额/换手率/量比/市盈率/信号/操作），展示当日关注股票。
- **选股器**：左侧4大分类（技术面/基本面/资金面/市场面）+ 11个子标签条件筛选，已选条件展示，选股结果列表。
- **异动寻龙**：12列数据表格（时间/代码/名称/现价/涨跌幅/涨速/成交额/换手率/量比/异动类型/原因/操作），展示当日异动股票。

#### 3.7 行情中枢（Rankings）
- **市场概览**：展示涨跌家数、涨停/跌停家数、市场总成交额等大盘数据。
- **行业资金流向**：展示各行业板块资金流入流出排名。
- **涨幅榜/跌幅榜/成交额榜/换手率榜**：多维度股票排行榜。

#### 3.8 复盘社区（Community）
- 精选复盘文章列表，展示文章标题、日期、摘要。
- 点击展开查看文章详情。
- 按日期分类展示复盘内容。

#### 3.9 明确排除的功能（不做）
| 排除项 | 说明 |
| --- | --- |
| 注册 / 登录 | 无任何账号体系，打开即用 |
| VIP / 付费订阅 | 所有功能对所有人开放，无权限分级、无隐藏字段 |
| 投票功能 | 无任何投票、点赞、评选 |
| 用户留言 / 交互 | 无评论、发帖、关注操作、模拟交易、消息通知等一切用户写操作 |
| 社区 / 社交 | 无用户生成内容，复盘社区为静态文章展示 |

### 4. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 运行环境 | 后端：Python **3.8+**（Flask）；前端：Vue 3 + Ant Design Vue + Vite |
| 兼容性 | 后端代码使用 Python 3.8 兼容语法（无 `match`、无 `|` 类型联合等 3.9+ 特性） |
| 响应速度 | 行情数据带 10 秒 TTL 缓存，避免高频请求上游接口 |
| 稳定性 | 上游接口异常时返回友好错误信息，页面不崩溃 |
| 前端体验 | 暗色终端风格，与参考站保持一致；桌面/移动端自适应 |
| 部署 | 单进程即可运行：后端同时托管 API 与前端静态资源 |
| 安全 | 无用户输入写入、无敏感数据；后端仅做数据聚合转发 |
| 合规 | 数据来源于公开行情接口，仅用于学习与个人参考，不构成投资建议 |

### 5. 技术架构

```
浏览器 (Vue3 + Ant Design Vue, Vite 构建)
        │  HTTP /api/*
        ▼
Flask 后端 (Python 3.8)
   ├── /api/health          健康检查
   ├── /api/indices         主要指数行情（A股/亚太/美股/期货）
   ├── /api/stock/search    股票检索（支持科创板/创业板）
   ├── /api/stock/quote     个股行情详情
   └── /api/stock/batch     批量行情查询
        │  短缓存 (TTL 10s) + 主备双数据源并发请求（谁先成功用谁，主源优先）
        ▼
主源：东方财富公开行情 (push2.eastmoney.com / searchapi / codetable)
备源：腾讯行情 (qt.gtimg.cn) —— 东财挂起/失败时快速兜底
```

- 前端通过 Vite 开发代理（`/api` → `:5000`）或同源部署访问后端。
- 后端对所有上游请求做 10 秒内存缓存，控制外部依赖频率。
- 双数据源：**主备并发请求**，东方财富与腾讯同时发起，优先取主源成功结果；
  东财挂起（连接超时）或失败时自动采用腾讯结果，避免用户长时间等待（实测主源不可达时
  响应约 0.6~0.9s，而串行重试需 4~5s）。各接口响应中带 `source` 字段
  （`eastmoney` / `tencent`）标识实际数据来源。
- 指数定义采用配置驱动：`config.INDICES` 中每个指数同时标注东财 secid 与腾讯代码，
  回退时按条目精确映射，避免市场间数据错位。
- 详情弹窗中的暗盘资金、个股分析、暗盘历史、异动清单等数据为模拟演示数据，真实数据需接入专用数据源。

### 6. 接口定义（REST）

| 方法 | 路径 | 参数 | 说明 |
| --- | --- | --- | --- |
| GET | `/api/health` | - | 健康检查 |
| GET | `/api/indices` | `market=cn\|asia\|us\|futures`（默认 cn） | 主要指数行情 |
| GET | `/api/stock/search` | `keyword=关键词&count=8` | 股票模糊检索 |
| GET | `/api/stock/quote` | `code=600000` | 个股行情详情 |
| GET | `/api/stock/batch` | `codes=600000,000001` | 批量行情查询 |

响应统一格式：`{"code": 0, "message": "ok", "data": {...}}`。

### 7. 数据来源与免责声明

- 行情数据来自东方财富 / 腾讯公开接口，仅作个人学习与技术演示用途，数据可能存在延迟或误差。
- 详情弹窗中的暗盘资金、个股分析、暗盘历史、异动清单等分析数据为模拟演示数据，不构成投资建议。
- 本项目不构成任何投资建议，据此操作风险自负。

---

## 二、目录结构

```
StockOSSecTools/
├── README.md                 # 本文档（需求文档）
├── requirements.txt          # 后端依赖（Python 3.8）
├── .gitignore
├── reference.png             # 参考网站详情弹窗截图
├── backend/
│   ├── app.py                # Flask 应用入口（API + 静态托管）
│   ├── config.py             # 配置：指数定义、缓存、数据源、前端路径
│   ├── services/
│   │   ├── __init__.py
│   │   ├── cache.py          # TTL 内存缓存
│   │   └── eastmoney.py      # 行情服务（东财+腾讯并发双源、检索、批量行情）
│   └── tests/
│       └── test_api.py       # 后端单元测试（mock 上游，21 项）
└── frontend/
    ├── package.json
    ├── vite.config.js        # 含 /api 开发代理
    ├── index.html
    ├── e2e-all.cjs           # 前端全功能 E2E 测试（68 项断言）
    ├── dist/                 # 构建产物（后端自动托管）
    └── src/
        ├── main.js           # 入口（挂载 Ant Design Vue 暗色主题）
        ├── App.vue           # 主应用（多视图切换 + 顶部/底部导航）
        ├── api/index.js      # 后端 API 封装
        ├── styles/global.css
        ├── components/
        │   ├── StockQuery.vue    # 个股查询（含详情弹窗四标签）
        │   ├── IndicesPanel.vue  # 主要指数（四市场 + 自动刷新）
        │   └── Watchlist.vue     # 关注清单（本地存储 + 可配置列）
        └── views/
            ├── Home.vue          # 首页
            ├── Tools.vue         # 工具页（个股查询 + 指数 + 关注清单）
            ├── Portfolio.vue     # 投资组合
            ├── Utility.vue       # 量化工具（今日关注/选股器/异动寻龙）
            ├── Rankings.vue      # 行情中枢
            └── Community.vue     # 复盘社区
```

## 三、运行说明

### 1. 后端（Python 3.8+）

```bash
cd backend
pip install -r ../requirements.txt   # 或 pip install Flask==2.3.3 flask-cors==3.0.10 requests==2.31.0
python app.py                         # 默认监听 0.0.0.0:5000
```

验证：浏览器访问 `http://127.0.0.1:5000/api/health` 返回 `{"code":0,...}` 即成功。

### 2. 前端（开发模式）

```bash
cd frontend
npm install
npm run dev                          # 默认 http://localhost:5173，/api 代理到 :5000
```

### 3. 生产部署（单进程）

```bash
cd frontend && npm install && npm run build   # 产物输出到 frontend/dist
cd ../backend && python app.py                # 后端自动托管 frontend/dist 静态页面
```

访问 `http://127.0.0.1:5000/` 即为完整页面（API 与页面同源）。

### 4. 测试

```bash
# 后端单元测试（mock 上游接口，无需网络，21 项）
cd backend && python -m unittest discover -s tests -v

# 前端全功能 E2E（68 项断言，需先启动后端并构建前端，使用本机 Edge/Chrome）
cd frontend && npm run build && npm run test:e2e
```

E2E 覆盖：导航（底部导航/Logo/首页模块/量化工具入口）、个股查询（名称搜索点选/代码查询/
无效输入提示/加关注）、详情弹窗（四标签/图表范围/图表周期/刷新/收藏/更多）、主要指数
（四个市场切换/自动刷新开关/手动刷新）、关注清单（展示/搜索添加/列配置/备注编辑/全选/
删除确认/批量添加）、量化工具（三个标签/搜索过滤/刷新/留列/详情弹窗/选股器分类与标签/
确定/收起展开/修改/导出 CSV）、行情中枢（六个标签/子标签）、组合与复盘页渲染、通知与设置按钮。

### 5. 注意事项

- 后端必须在 `backend` 目录下运行，否则会出现模块导入错误。
- 若网页无法打开，先检查后端进程是否在运行，可访问 `http://127.0.0.1:5000/api/health` 验证。
- 关注清单数据存储在浏览器 localStorage 中，清除浏览器数据会丢失关注列表。
- 详情弹窗中的分析数据为模拟演示数据，如需真实数据需接入专用数据源。
